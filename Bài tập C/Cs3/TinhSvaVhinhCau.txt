#include <stdio.h>
count float PI 3.14

int main() 
{  
    float r;
    float s;
    float v;
    printf("Nhap R: ");
    scanf("%f", &r);
    s = 4 * PI * r * r;
    v = (4/3) * PI * r * r * r;
    printf("S = %0.1f", s);
    printf("\nV = %0.1f", v);
   
    return 0;
}