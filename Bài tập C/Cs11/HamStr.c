#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char str1[100];
    char str2[100];

    //char *pC;

    //--------------------------------------------------------------
    // Độ dài của chuỗi
    // printf("Nhập chuỗi thứ nhất: ");
    // gets(str1);
    // printf("Nhập chuỗi thứ hai: ");
    // gets(str2);
    // printf("Trả về độ dài của chuỗi str1: %d", strlen(str1));

    // -------------------------------------------------------------
    // Coppy 2 chuỗi
    // printf("Nhập chuỗi thứ nhất: ");
    // gets(str1);
    // printf("Nhập chuỗi thứ hai: ");
    // gets(str2);

    // printf("Coppy hai chuỗi: ");
    // strcpy(str1, str2);
    // puts(str1);

    //---------------------------------------------------------------
    // printf("Xác định 1 kí tự trong chuỗi: ");
    // gets(str1);
    // char c;
    // printf("Nhập kí tự C: ");
    // scanf("%c", &c);
    // pC = strchr(str1, c);
    // printf("%x", pC);

    //---------------------------------------------------------------
    // printf("So sánh 2 chuỗi: ");
    // int kq = strcmp(str1, str2);
    // if (kq < 0)
    // {
    //     printf("%s < %s", str1, str2);
    // }
    // else if (kq == 0)
    // {
    //     printf("%s == %s", str1, str2);
    // }
    // else
    // {
    //     printf("%s > %s", str1, str2);
    // }

    //----------------------------------------------------------------
    // Đảo ngược chuỗi
    // printf("Chuỗi vừa nhập: ");
    // puts(str1);

    //printf("\nChuỗi đảo ngược: %s", strrev(str1));

    return 0;
}
