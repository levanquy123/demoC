#include <stdio.h>

int main(int argc, char const *argv[])
{

    int n;
    printf("Nhap n: ");
    scanf("%d",&n);

    int result = 0;
    while (n > 0)

    {
        result += n%10;
        n /=10;
    }

    printf("Tong cac chu so la: %d",result);
   
    return 0;
}