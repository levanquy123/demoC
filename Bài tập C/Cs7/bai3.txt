#include "stdio.h"

int main(int argc, char const *argv[])
{
    int array[10];
    int i = 0;
    short flag = 0;
    int min = 0, minCount = 0;
    int max = 0, maxCount = 0;

    for (i = 0; i < 10; i++)
    {
        printf("Input array[%d]: ", i);
        scanf("%d", &array[i]);
        if (flag == 0)
        {
            min = array[i];
            max = array[i];
            flag = 1;
        }
        else if (min > array[i])
        {
            min = array[i];
        }
        else if (max < array[i])
        {
            max = array[i];
        }
        else
        {
            i++;
        }
    }

    for (int j = 0; j < 10; j++)
    {
        if (min == array[j])
        {
            minCount++;
        }
        else if (max == array[j])
        {
            maxCount++;
        }
        else
        {
            j++;
        }
    }

    printf("min:%d\t,minCount:%d\n", min, minCount);
    printf("\n");
    printf("max:%d\t,maxCount:%d", max, maxCount);

    return 0;
}
